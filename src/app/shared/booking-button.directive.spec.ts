import { BookingButtonDirective } from './booking-button.directive';

describe('BookingButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingButtonDirective();
    expect(directive).toBeTruthy();
  });
});
